/**
 * Auth module — pas de modèle MongoDB pour l'instant.
 * L'admin est authentifié via ADMIN_EMAIL / ADMIN_PASSWORD (.env) + JWT.
 * Ajouter ici un modèle AdminUser si tu veux des comptes en base plus tard.
 */
export {};
